import { observer } from 'mobx-react-lite';
import { Box, Text } from '@chakra-ui/react';

export const SpaceCreationOriginSelect = observer(
  function SpaceCreationOriginSelect() {
    return (
      <Box>
        <Text></Text>
      </Box>
    );
  }
);
